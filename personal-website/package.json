{
  "name": "personal-website-monorepo",
  "private": true,
  "workspaces": [
    "landing",
    "blog"
  ],
  "scripts": {
    "dev:landing": "npm --prefix landing run dev",
    "dev:blog": "npm --prefix blog run dev",
    "build:all": "npm --prefix landing run build && npm --prefix blog run build"
  }
}
